# PrakWeb2

This repository contains a simple HTML/CSS website project with the following structure:

- `html/` — website pages (`index.html`, `profile.html`, `portfolio.html`)
- `css/` — styling (`styles.css`)
- `img/` — image assets
- `icon/` — icon assets
- `video/` — video assets

## What was fixed

- Renamed `html/portofolio.html` to `html/portfolio.html`
- Updated all internal links to use the corrected filename
- Normalized the tracked filename case for `html/Profile.html`
- Fixed image path casing in the portfolio page
- Removed the old remote reference to the deleted GitHub repository

## Next step to publish

1. Create a new public repository on GitHub.
2. Add it as the remote origin:
   ```bash
   git remote add origin https://github.com/<username>/<repo>.git
   git branch -M main
   git push -u origin main
   ```

```

If you want, I can also help you with the exact GitHub repository name and push commands once the repo is created.
```
